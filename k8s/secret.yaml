# NOTE: This is a template. Do NOT commit real secrets.
# Apply via: kubectl create secret generic hragent-secrets --from-env-file=.env.production -n hragent
apiVersion: v1
kind: Secret
metadata:
  name: hragent-secrets
  namespace: hragent
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  SESSION_SECRET: "REPLACE_ME"
  SENTRY_DSN: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  GOOGLE_API_KEY: "REPLACE_ME"
  SLACK_API_TOKEN: "REPLACE_ME"
  MAILTRAP_USERNAME: "REPLACE_ME"
  MAILTRAP_PASSWORD: "REPLACE_ME"
  ZOOM_API_KEY: "REPLACE_ME"
  ZOOM_API_SECRET: "REPLACE_ME"
